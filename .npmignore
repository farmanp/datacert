# Ignore everything by default
*

# Include CLI distribution (with bundled web UI)
!dist/cli/**

# Include package metadata
!package.json
!README.md
!LICENSE

# Exclude dev/build artifacts that might slip through
dist/cli/**/*.map
dist/cli/**/*.d.ts

# Note: Web UI is bundled at dist/cli/web/ for local serving
